# Disabled — CI runs via `nix run .#apps.<sys>.ci-*` on the blackmatter
# aggregator instead of GitHub Actions. Re-enable by removing the leading
# "# " from each line below.
#
# name: CI
# 
# on:
#   pull_request:
#   push:
#     branches: [main]
# 
# jobs:
#   check:
#     uses: pleme-io/substrate/.github/workflows/blackmatter-component-check.yml@main
